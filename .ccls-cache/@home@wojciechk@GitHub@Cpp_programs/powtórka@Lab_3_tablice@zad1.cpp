#include <iostream>
#include <ctime>
#include <cstdlib>

using namespace std;

int main()
{
  srand(time(nullptr));
  const int N = 15;
  char arr[N];
  for(int i = 0; i < N; i++)
  {
    arr[i] = rand() %  xx
  }

  return 0;
}
